PROGRAM COMPLEX3;

DATA vars = A, B, C, D, E, R;

PROC main =
    MOVE '0', R;
    { (A OR B) AND (C OR D) }
    IF (A = '1' OR B = '2') AND (C = '3' OR D = '4') THEN MOVE '1', R;
    EXIT R;
END_PROC
