PROGRAM T_FETCH;

COPY XXCGTSYS;

DATA
  vars =
    RDA0;

PROC main =
    { Basic FETCH }
    FETCH RDA0;

    { FETCH with literal }
    FETCH 'OBJECT';

    { FETCH with P register }
    MOVE 'TESTOBJ', P0;
    FETCH P0;

    { FETCH with GEV }
    FETCH SYS_RETURN_CODE;

    RETURN;
END_PROC
