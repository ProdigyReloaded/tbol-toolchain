PROGRAM T_KILL;

COPY XXCGTSYS;

DATA
  vars =
    RDA0;

PROC main =
    { KILL with RDA }
    KILL RDA0;

    { KILL with literal }
    KILL 'PROCESS';

    { KILL with P register }
    MOVE 'TESTPROC', P0;
    KILL P0;

    { KILL with GEV }
    KILL SYS_RETURN_CODE;

    RETURN;
END_PROC
